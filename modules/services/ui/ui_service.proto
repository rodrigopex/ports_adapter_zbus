syntax = "proto3";

import "nanopb.proto";
import "service.proto";

option (nanopb_fileopt).anonymous_oneof = true;

message MsgUIService {
  message Invoke {
    oneof ui_invoke {
      Void blink = 1;
    }
  }

  message Report {
    oneof ui_report {
      Void error_code = 1;
    }
  }
}
