/* GENERATED FILE - DO NOT EDIT */
/* Report helpers for {{ service_name }} implementation */

#include "{{ service_name }}_interface.h"

{%- for field in report_fields %}
{%- if field.is_empty %}

static inline int {{ service_name }}_report_{{ field.name }}(k_timeout_t timeout)
{
	return zbus_chan_pub(&chan_{{ service_name }}_report,
			     &(struct msg_{{ service_name }}_report){
				     .which_{{ report_oneof_name }} = MSG_{{ service_name_upper }}_REPORT_{{ field.name|upper }}_TAG},
			     timeout);
}
{%- else %}
{%- if field.message_type == 'MsgServiceStatus' %}

static inline int {{ service_name }}_report_{{ field.name }}(const struct msg_service_status *status,
					     k_timeout_t timeout)
{
	return zbus_chan_pub(
		&chan_{{ service_name }}_report,
		&(struct msg_{{ service_name }}_report){
			.which_{{ report_oneof_name }} = MSG_{{ service_name_upper }}_REPORT_{{ field.name|upper }}_TAG, .status = *status},
		timeout);
}
{%- elif field.message_type == 'Config' %}

static inline int {{ service_name }}_report_{{ field.name }}(const struct msg_{{ service_name }}_config *config,
					     k_timeout_t timeout)
{
	return zbus_chan_pub(
		&chan_{{ service_name }}_report,
		&(struct msg_{{ service_name }}_report){
			.which_{{ report_oneof_name }} = MSG_{{ service_name_upper }}_REPORT_{{ field.name|upper }}_TAG, .config = *config},
		timeout);
}
{%- else %}

static inline int {{ service_name }}_report_{{ field.name }}(const struct msg_{{ service_name }}_{{ field.message_type|camel_to_snake }} *{{ field.name }},
					     k_timeout_t timeout)
{
	return zbus_chan_pub(
		&chan_{{ service_name }}_report,
		&(struct msg_{{ service_name }}_report){
			.which_{{ report_oneof_name }} = MSG_{{ service_name_upper }}_REPORT_{{ field.name|upper }}_TAG, .{{ field.name }} = *{{ field.name }}},
		timeout);
}
{%- endif %}
{%- endif %}
{%- endfor %}
